# Prometheus scrape config for metrics from the OTel Collector.
# Run Prometheus: prometheus --config.file=prometheus.yml
# Collector must be running with metrics pipeline exporting to prometheus (port 8889).
scrape_configs:
  - job_name: "otel-collector"
    scrape_interval: 15s
    static_configs:
      - targets: ["localhost:8889"]
